# Copie para backend/.env (ou exporte antes de rodar Prisma na pasta backend)
DATABASE_URL="postgresql://pf:pf@localhost:5432/personal_futebol?schema=public"

# API
PORT=3333
JWT_SECRET=change-me-in-development
JWT_EXPIRES_DAYS=7
# Uma origem ou várias separadas por vírgula (ex.: app Vercel + previews).
CORS_ORIGIN=http://localhost:5173
